export const DEFAULT_TIMER_SIZE = 200;

export const DEFAULT_RING_WIDTH = 20;
export const DEFAULT_RING_ACTIVE_COLOR = '#f00';
export const DEFAULT_RING_INACTIVE_COLOR = '#ccc';

export const DEFAULT_INITIAL_ROTATION = 0;

export const DEFAULT_IS_EDITABLE = true;

export const DEFAULT_TICKS_COUNT = 60;
export const DEFAULT_TICK_HEIGHT = 10;
export const DEFAULT_TICK_WIDTH = 1;
export const DEFAULT_TICK_COLOR = '#000';

export const DEFAULT_MINUTES_PER_CIRCLE = 60;

export const DEFAULT_HINT_SIZE = 120;
export const DEFAULT_HINT_COLOR = '#000';
export const DEFAULT_HINT_ENABLED_ROTATION = true;
